# TODO
- [x] Habilitar carregamento automático do `import.sql` pelo Spring (spring.sql.init.*) no `src/main/resources/application.properties`.
- [ ] Reiniciar a aplicação e confirmar via `GET /api/equipamentos` e `GET /api/fabricantes` que agora retornam dados.


